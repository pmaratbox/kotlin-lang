# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Built with kotlinx.coroutines Flow using the `zip` operator to pair `flowOf(1,2,3)` with `flowOf(10,20,30)`.

## Run

    kotlin ZipStreams.main.kts
