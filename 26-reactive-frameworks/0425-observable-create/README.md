# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses kotlinx.coroutines Flow's `flowOf` creation operator with `onCompletion` for the completion callback.

## Run

    kotlin ObservableCreate.main.kts
