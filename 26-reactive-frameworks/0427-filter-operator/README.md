# 0427 — Filter Operator

Use the library's filter operator to keep the even values of 1..6. Built with kotlinx.coroutines Flow using the `filter` operator on `(1..6).asFlow()`.

## Run

    kotlin FilterOperator.main.kts
