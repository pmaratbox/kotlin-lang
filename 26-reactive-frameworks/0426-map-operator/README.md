# 0426 — Map Operator

Use the library's map operator to double each value of a 1..4 stream. Built with kotlinx.coroutines Flow using `flowOf` and the `map` operator.

## Run

    kotlin MapOperator.main.kts
