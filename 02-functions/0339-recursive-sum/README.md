# 0339 — Recursive List Sum

Recursively sum the list [1,2,3,4], printing `10`. A single-expression Kotlin function splits head and tail with `first()` and `drop(1)`.

## Run

    kotlinc RecursiveSum.kt -include-runtime -d recursivesum.jar && java -jar recursivesum.jar
