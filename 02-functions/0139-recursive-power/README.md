# 0139 — Recursive Power

Compute 2 raised to the 10th power recursively and print `1024`. A single-expression `fun` makes the recursive definition read like its math.

## Run

    kotlinc Power.kt -include-runtime -d power.jar && java -jar power.jar
