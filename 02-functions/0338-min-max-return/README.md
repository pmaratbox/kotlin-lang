# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. Kotlin returns both values together as a `Pair` destructured at the call site.

## Run

    kotlinc MinMaxReturn.kt -include-runtime -d minmaxreturn.jar && java -jar minmaxreturn.jar
