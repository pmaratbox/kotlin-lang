# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Building a `List<Int>` recursively and `joinToString(" ")` keeps the output on one line.

## Run

    kotlinc Recursion.kt -include-runtime -d recursion.jar && java -jar recursion.jar
