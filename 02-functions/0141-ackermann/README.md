# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. A `when` expression cleanly encodes the three Ackermann cases.

## Run

    kotlinc Ackermann.kt -include-runtime -d ackermann.jar && java -jar ackermann.jar
