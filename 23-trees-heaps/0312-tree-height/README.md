# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. Kotlin expresses the recurrence as a single-expression function with `maxOf`.

## Run

    kotlinc TreeHeight.kt -include-runtime -d treeheight.jar && java -jar treeheight.jar
