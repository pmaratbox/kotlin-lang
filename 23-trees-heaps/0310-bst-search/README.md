# 0310 — BST Search

Insert 5,3,8,1,4 into a BST, then search for 4 (yes) and 6 (no), printing `yes no`. Kotlin's `when` expression drives the iterative descent.

## Run

    kotlinc BstSearch.kt -include-runtime -d bstsearch.jar && java -jar bstsearch.jar
