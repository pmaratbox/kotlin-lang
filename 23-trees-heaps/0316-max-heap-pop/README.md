# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Kotlin uses `PriorityQueue` with `reverseOrder()` to get max-heap behavior.

## Run

    kotlinc MaxHeapPop.kt -include-runtime -d maxheappop.jar && java -jar maxheappop.jar
