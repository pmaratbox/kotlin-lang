# 0317 — Heap Sort

Heap-sort [5,3,8,1,4] ascending, printing `1 3 4 5 8`. Kotlin sorts an `IntArray` in place with a max-heap build and repeated sift-down.

## Run

    kotlinc HeapSort.kt -include-runtime -d heapsort.jar && java -jar heapsort.jar
