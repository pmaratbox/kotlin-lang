# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Kotlin uses recursive bound checks with nullable `Node?` references.

## Run

    kotlinc ValidateBst.kt -include-runtime -d validatebst.jar && java -jar validatebst.jar
