# 0186 — Reverse Integer

Reverse the digits of 1234 and print `4321`. Pop last digit, build reversed via *10 in a `while` loop.

## Run

    kotlinc Integer.kt -include-runtime -d integer.jar && java -jar integer.jar
