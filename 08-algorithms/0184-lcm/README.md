# 0184 — Least Common Multiple

Compute lcm(4,6) via gcd and print `12`. lcm = a/gcd(a,b)*b using a recursive single-expression `gcd`.

## Run

    kotlinc Lcm.kt -include-runtime -d lcm.jar && java -jar lcm.jar
