# 0181 — Insertion Sort

Insertion-sort [5,1,4,2,8] ascending, printing `1 2 4 5 8`. Shift larger elements right and insert with a `while` loop.

## Run

    kotlinc Sort.kt -include-runtime -d sort.jar && java -jar sort.jar
