# 0180 — Bubble Sort

Bubble-sort [5,1,4,2,8] ascending, printing `1 2 4 5 8`. Repeated adjacent swaps over an `IntArray`.

## Run

    kotlinc Sort.kt -include-runtime -d sort.jar && java -jar sort.jar
