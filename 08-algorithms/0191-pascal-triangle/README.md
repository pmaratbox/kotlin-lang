# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. Each entry is the sum of the two above; print 4 rows by mapping adjacent pairs.

## Run

    kotlinc Triangle.kt -include-runtime -d triangle.jar && java -jar triangle.jar
