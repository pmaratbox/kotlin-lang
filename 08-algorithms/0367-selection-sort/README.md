# 0367 — Selection Sort

Selection-sort [5,1,4,2] ascending, printing `1 2 4 5`. Track the minimum index with a simple inner loop over an `IntArray`.

## Run

    kotlinc SelectionSort.kt -include-runtime -d selectionsort.jar && java -jar selectionsort.jar
