# 0183 — Prime Factorization

Factor 60 into primes, printing `2 2 3 5`. Divide out 2s, then odd factors; print with multiplicity ascending via a `MutableList`.

## Run

    kotlinc Factorization.kt -include-runtime -d factorization.jar && java -jar factorization.jar
