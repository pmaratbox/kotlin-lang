# 0187 — Count Set Bits

Count the set bits (popcount) of 13 and print `3`. 13 = 1101 -> 3 ones via the stdlib `Int.countOneBits()`.

## Run

    kotlinc SetBits.kt -include-runtime -d setbits.jar && java -jar setbits.jar
