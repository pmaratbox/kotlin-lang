# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. Lomuto partition recursing into only the side holding rank `k`.

## Run

    kotlinc Quickselect.kt -include-runtime -d quickselect.jar && java -jar quickselect.jar
