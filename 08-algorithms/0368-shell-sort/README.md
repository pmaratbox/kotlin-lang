# 0368 — Shell Sort

Shell-sort [5,2,8,1,9,3] ascending, printing `1 2 3 5 8 9`. Gap-based insertion sort halving the gap each pass.

## Run

    kotlinc ShellSort.kt -include-runtime -d shellsort.jar && java -jar shellsort.jar
