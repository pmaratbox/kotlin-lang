# 0371 — Counting Sort

Counting-sort [3,1,2,3,1] ascending, printing `1 1 2 3 3`. Tally each value into an `IntArray` then emit in value order.

## Run

    kotlinc CountingSort.kt -include-runtime -d countingsort.jar && java -jar countingsort.jar
