# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Recursively fix each head element in index order so output is lexicographic.

## Run

    kotlinc Permutations.kt -include-runtime -d permutations.jar && java -jar permutations.jar
