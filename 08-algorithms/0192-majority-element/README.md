# 0192 — Majority Element

Find the majority element of [2,2,1,2,3,2] using Boyer-Moore voting and print `2`. Candidate + count voting; 2 occurs 4/6 times.

## Run

    kotlinc Element.kt -include-runtime -d element.jar && java -jar element.jar
