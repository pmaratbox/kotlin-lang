# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Split on underscores, keep the first word, and `replaceFirstChar` to capitalize the rest.

## Run

    kotlinc SnakeToCamel.kt -include-runtime -d snaketocamel.jar && java -jar snaketocamel.jar
