# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `String.reversed()` returns the mirror image, compared to the original with `==` (structural equality).

## Run

    kotlinc Palindrome.kt -include-runtime -d palindrome.jar && java -jar palindrome.jar
