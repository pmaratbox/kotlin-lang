# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Use the language interpolation/format facility.

## Run

    kotlinc Interpolation.kt -include-runtime -d interpolation.jar && java -jar interpolation.jar
