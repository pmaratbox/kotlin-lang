# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. Keep a seen-set; preserve first-seen order.

## Run

    kotlinc DuplicateChars.kt -include-runtime -d duplicatechars.jar && java -jar duplicatechars.jar
