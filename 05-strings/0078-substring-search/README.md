# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `String.indexOf` returns the first match index (or `-1`); `indexOf` is part of the Kotlin stdlib over `CharSequence`.

## Run

    kotlinc SubstringSearch.kt -include-runtime -d substring.jar && java -jar substring.jar
