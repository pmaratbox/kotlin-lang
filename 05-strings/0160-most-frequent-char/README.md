# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. Count chars; pick the max (l appears twice).

## Run

    kotlinc FrequentChar.kt -include-runtime -d frequentchar.jar && java -jar frequentchar.jar
