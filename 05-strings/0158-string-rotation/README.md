# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. b is a rotation of a iff same length and b is a substring of a+a.

## Run

    kotlinc Rotation.kt -include-runtime -d rotation.jar && java -jar rotation.jar
