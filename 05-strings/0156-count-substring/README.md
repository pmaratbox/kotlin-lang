# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Scan for non-overlapping matches.

## Run

    kotlinc Substring.kt -include-runtime -d substring.jar && java -jar substring.jar
