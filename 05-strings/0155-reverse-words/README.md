# 0155 — Reverse Words

Reverse the order of words in "hello world", printing `world hello`. Split on spaces, reverse the list, join with a space.

## Run

    kotlinc Words.kt -include-runtime -d words.jar && java -jar words.jar
