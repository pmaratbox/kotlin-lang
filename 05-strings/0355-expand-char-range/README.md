# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. A Kotlin `CharRange` (`start..end`) iterates inclusively from the start to the end char.

## Run

    kotlinc ExpandCharRange.kt -include-runtime -d expandcharrange.jar && java -jar expandcharrange.jar
