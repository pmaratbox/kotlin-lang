# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `String.reversed()` returns the characters in reverse; like `StringBuilder`, it reverses the UTF-16 `Char`s.

## Run

    kotlinc ReverseString.kt -include-runtime -d reverse.jar && java -jar reverse.jar
