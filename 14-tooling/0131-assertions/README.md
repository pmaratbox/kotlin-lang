# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A small `check` helper throws an `AssertionError` on mismatch.

## Run

    kotlinc Assertions.kt -include-runtime -d assertions.jar && java -jar assertions.jar
