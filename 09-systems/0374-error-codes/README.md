# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A `mapOf` gives an idiomatic code-to-message lookup.

## Run

    kotlinc ErrorCodes.kt -include-runtime -d errorcodes.jar && java -jar errorcodes.jar
