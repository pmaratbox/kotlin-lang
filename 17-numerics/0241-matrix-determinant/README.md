# 0241 — 2x2 Determinant

Compute the determinant of [[1,2],[3,4]], printing `-2`. Nested `IntArray`s hold the matrix rows.

## Run

    kotlinc MatrixDeterminant.kt -include-runtime -d matrixdeterminant.jar && java -jar matrixdeterminant.jar
