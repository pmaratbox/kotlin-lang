# 0240 — Complex Multiplication

Multiply the complex numbers (1+2i) and (3+4i), printing the real and imaginary parts `-5 10`. An operator overload on a `data class` defines complex multiplication.

## Run

    kotlinc ComplexMultiply.kt -include-runtime -d complexmultiply.jar && java -jar complexmultiply.jar
