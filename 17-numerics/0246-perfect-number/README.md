# 0246 — Perfect Number Check

Test whether 6 (yes) and 8 (no) are perfect numbers, printing `yes no`. A range `filter`/`sum` collects the proper divisors.

## Run

    kotlinc PerfectNumber.kt -include-runtime -d perfectnumber.jar && java -jar perfectnumber.jar
