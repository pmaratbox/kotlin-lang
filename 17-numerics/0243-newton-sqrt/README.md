# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. `String.format` with `Locale.US` fixes the decimal point.

## Run

    kotlinc NewtonSqrt.kt -include-runtime -d newtonsqrt.jar && java -jar newtonsqrt.jar
