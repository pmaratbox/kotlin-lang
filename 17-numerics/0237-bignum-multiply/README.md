# 0237 — Big Number Multiplication

Multiply 123 by 456 with schoolbook long multiplication on digit strings, printing `56088`. An `IntArray` accumulates partial products with carries.

## Run

    kotlinc BignumMultiply.kt -include-runtime -d bignummultiply.jar && java -jar bignummultiply.jar
