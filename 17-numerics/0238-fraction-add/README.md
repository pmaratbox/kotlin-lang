# 0238 — Fraction Addition

Add the fractions 1/2 and 1/3 and print the reduced result `5/6`. A recursive `gcd` reduces the summed fraction.

## Run

    kotlinc FractionAdd.kt -include-runtime -d fractionadd.jar && java -jar fractionadd.jar
