# 0236 — Big Number Addition

Add the large numbers 999999999999 and 1 as digit strings, printing `1000000000000`. Kotlin's `StringBuilder` builds the digits in reverse before flipping.

## Run

    kotlinc BignumAdd.kt -include-runtime -d bignumadd.jar && java -jar bignumadd.jar
