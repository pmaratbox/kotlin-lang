# 0239 — Reduce a Fraction

Reduce the fraction 6/8 to lowest terms `3/4`. String templates interpolate the divided numerator and denominator.

## Run

    kotlinc FractionReduce.kt -include-runtime -d fractionreduce.jar && java -jar fractionreduce.jar
