# 0265 — Vending Machine

Insert coins 10, 10, 5 to reach the price 25 and dispense, printing `dispensed`. A running total over a `listOf` of coins triggers the dispensing state in Kotlin.

## Run

    kotlinc VendingMachine.kt -include-runtime -d vendingmachine.jar && java -jar vendingmachine.jar
