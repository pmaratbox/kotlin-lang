# 0419 — Catch Error

Implement catchError that, on an error from the source, switches to a fallback stream. Kotlin lambdas with default parameters keep the observer triple concise.

## Run

    kotlinc CatchError.kt -include-runtime -d catcherror.jar && java -jar catcherror.jar
