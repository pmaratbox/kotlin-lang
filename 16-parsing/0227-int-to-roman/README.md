# 0227 — Integer to Roman

Format the integer 14 as the Roman numeral `XIV`. A list of value-symbol pairs is greedily subtracted from the number.

## Run

    kotlinc IntToRoman.kt -include-runtime -d inttoroman.jar && java -jar inttoroman.jar
