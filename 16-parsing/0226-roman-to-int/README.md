# 0226 — Roman to Integer

Parse the Roman numeral "XIV" to its value `14`. A value map lets us subtract when a smaller numeral precedes a larger one.

## Run

    kotlinc RomanToInt.kt -include-runtime -d romantoint.jar && java -jar romantoint.jar
