# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. `removeSurrounding` strips the brackets and `sumOf` folds the parsed ints.

## Run

    kotlinc JsonArraySum.kt -include-runtime -d jsonarraysum.jar && java -jar jsonarraysum.jar
