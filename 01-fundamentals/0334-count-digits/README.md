# 0334 — Count Digits

Count the digits of 90210, printing `5`. Idiomatic Kotlin divides by ten in a do/while loop until the value reaches zero.

## Run

    kotlinc CountDigits.kt -include-runtime -d countdigits.jar && java -jar countdigits.jar
