# 0137 — Triangle Pattern

Print a left-aligned triangle of asterisks of height 3. `String.repeat(i)` builds each row without an inner loop.

## Run

    kotlinc Pattern.kt -include-runtime -d pattern.jar && java -jar pattern.jar
