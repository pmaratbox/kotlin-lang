# 0136 — Sign Function

Print the sign (-1, 0, or 1) of -5, 0, and 5: `-1 0 1`. A subjectless `when` returns the sign directly as an expression.

## Run

    kotlinc Function.kt -include-runtime -d function.jar && java -jar function.jar
