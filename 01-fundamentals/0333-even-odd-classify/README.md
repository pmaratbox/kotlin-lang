# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. Idiomatic Kotlin maps the list to labels and joins them with a space.

## Run

    kotlinc EvenOddClassify.kt -include-runtime -d evenoddclassify.jar && java -jar evenoddclassify.jar
