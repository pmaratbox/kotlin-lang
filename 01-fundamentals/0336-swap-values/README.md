# 0336 — Swap Two Values

Swap a=1 and b=2 (without bitwise tricks) and print `2 1`. Idiomatic Kotlin swaps in place with the `also` scope function.

## Run

    kotlinc SwapValues.kt -include-runtime -d swapvalues.jar && java -jar swapvalues.jar
