# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A `when` expression with no subject reads like a clean cascade of guards.

## Run

    kotlinc FizzBuzz.kt -include-runtime -d fizzbuzz.jar && java -jar fizzbuzz.jar
