# 0335 — Iterative Factorial

Compute 5! iteratively, printing `120`. Idiomatic Kotlin accumulates the product over a `1..5` range loop.

## Run

    kotlinc FactorialIterative.kt -include-runtime -d factorialiterative.jar && java -jar factorialiterative.jar
