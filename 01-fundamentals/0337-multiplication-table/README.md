# 0337 — Multiplication Table

Print the 3x3 multiplication table, each row space-separated. Idiomatic Kotlin builds each row by joining the inner range's products with a space.

## Run

    kotlinc MultiplicationTable.kt -include-runtime -d multiplicationtable.jar && java -jar multiplicationtable.jar
