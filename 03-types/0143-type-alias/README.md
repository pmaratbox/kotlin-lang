# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. Kotlin's `typealias` introduces a name for an existing type without creating a new one.

## Run

    kotlinc Alias.kt -include-runtime -d alias.jar && java -jar alias.jar
