# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. The result indexes into a `listOf` of weekday names.

## Run

    kotlinc OfWeekZeller.kt -include-runtime -d ofweekzeller.jar && java -jar ofweekzeller.jar
