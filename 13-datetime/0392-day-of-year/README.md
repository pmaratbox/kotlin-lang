# 0392 — Day of Year

Compute the day-of-year of 2000-03-01 (a leap year), printing `61`. Summing an `IntArray` of month lengths up to the target month is idiomatic Kotlin.

## Run

    kotlinc DayOfYear.kt -include-runtime -d dayofyear.jar && java -jar dayofyear.jar
