# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. A single boolean expression with Kotlin's `%` operator captures the rule.

## Run

    kotlinc Year.kt -include-runtime -d year.jar && java -jar year.jar
