# 0394 — Add Minutes to Time

Add 90 minutes to 10:45 and print the resulting 24-hour time `12:15`. Kotlin's `String.format` with `%02d` zero-pads the hours and minutes.

## Run

    kotlinc TimeAddMinutes.kt -include-runtime -d timeaddminutes.jar && java -jar timeaddminutes.jar
