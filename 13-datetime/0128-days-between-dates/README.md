# 0128 — Days Between Dates

Compute the number of days from 2000-01-01 to 2000-12-31 and print `365`. The days-from-civil algorithm yields a `Long` serial day count to subtract.

## Run

    kotlinc BetweenDates.kt -include-runtime -d betweendates.jar && java -jar betweendates.jar
