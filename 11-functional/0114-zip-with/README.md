# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Kotlin's `zip` with a transform pairs elements and adds them.

## Run

    kotlinc With.kt -include-runtime -d with.jar && java -jar with.jar
