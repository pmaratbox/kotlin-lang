# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Kotlin's `sortedBy` ranks indices by their suffix text in one line.

## Run

    kotlinc SuffixArray.kt -include-runtime -d suffixarray.jar && java -jar suffixarray.jar
