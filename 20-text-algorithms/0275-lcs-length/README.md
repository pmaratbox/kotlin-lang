# 0275 — Longest Common Subsequence

Compute the LCS length of "ABCBDAB" and "BDCAB", printing `4`. Kotlin's `maxOf` keeps the recurrence readable.

## Run

    kotlinc LcsLength.kt -include-runtime -d lcslength.jar && java -jar lcslength.jar
