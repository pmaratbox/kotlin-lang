# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Kotlin's `groupBy` keys each word by its sorted letters in one expression.

## Run

    kotlinc GroupAnagrams.kt -include-runtime -d groupanagrams.jar && java -jar groupanagrams.jar
