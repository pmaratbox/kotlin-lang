# 0281 — Z-Function

Compute the Z-array of "aaaa" (excluding z[0]) and print `3 2 1`. Kotlin's `drop(1)` skips z[0] before joining.

## Run

    kotlinc ZFunction.kt -include-runtime -d zfunction.jar && java -jar zfunction.jar
