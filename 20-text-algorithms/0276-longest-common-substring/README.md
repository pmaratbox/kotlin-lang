# 0276 — Longest Common Substring

Find the longest common contiguous substring of "abcde" and "xbcdy", printing `bcd`. Kotlin's `substring` slices the winning run directly.

## Run

    kotlinc LongestCommonSubstring.kt -include-runtime -d longestcommonsubstring.jar && java -jar longestcommonsubstring.jar
