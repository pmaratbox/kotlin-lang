# 0274 — Levenshtein Distance

Compute the edit distance between "kitten" and "sitting", printing `3`. A 2D `Array<IntArray>` makes the DP table concise.

## Run

    kotlinc Levenshtein.kt -include-runtime -d levenshtein.jar && java -jar levenshtein.jar
