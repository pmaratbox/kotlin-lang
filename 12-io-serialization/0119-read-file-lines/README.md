# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. Kotlin's `File.readLines()` returns the lines as a list to count.

## Run

    kotlinc FileLines.kt -include-runtime -d filelines.jar && java -jar filelines.jar
