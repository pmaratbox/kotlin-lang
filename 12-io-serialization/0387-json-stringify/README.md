# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. A Kotlin triple-quoted string interpolates fields with no extra spaces.

## Run

    kotlinc JsonStringify.kt -include-runtime -d jsonstringify.jar && java -jar jsonstringify.jar
