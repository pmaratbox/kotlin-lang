# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. `all` and `any` take predicates and short-circuit.

## Run

    kotlinc Any.kt -include-runtime -d any.jar && java -jar any.jar
