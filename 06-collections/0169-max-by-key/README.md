# 0169 — Max By Key

From ["a","bbb","cc"] pick the longest string, printing `bbb`. `maxByOrNull { it.length }` returns the element with the greatest key.

## Run

    kotlinc ByKey.kt -include-runtime -d bykey.jar && java -jar bykey.jar
