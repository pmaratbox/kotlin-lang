# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Subtracting a `Set` keeps the first list's order while removing matches.

## Run

    kotlinc Difference.kt -include-runtime -d difference.jar && java -jar difference.jar
