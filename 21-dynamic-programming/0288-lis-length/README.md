# 0288 — Longest Increasing Subsequence

Compute the LIS length of [10,9,2,5,3,7,101,18], printing `4`. Kotlin's `IntArray.max()` reads off the best subsequence ending anywhere.

## Run

    kotlinc LisLength.kt -include-runtime -d lislength.jar && java -jar lislength.jar
