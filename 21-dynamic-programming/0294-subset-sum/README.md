# 0294 — Subset Sum

Decide whether some subset of [3,34,4,12,5,2] sums to 9, printing `yes`. A `BooleanArray` reachability table keeps the DP compact in Kotlin.

## Run

    kotlinc SubsetSum.kt -include-runtime -d subsetsum.jar && java -jar subsetsum.jar
