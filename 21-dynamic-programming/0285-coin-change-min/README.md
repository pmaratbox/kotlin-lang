# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. An `IntArray` seeded with a sentinel makes the bottom-up DP concise in Kotlin.

## Run

    kotlinc CoinChangeMin.kt -include-runtime -d coinchangemin.jar && java -jar coinchangemin.jar
