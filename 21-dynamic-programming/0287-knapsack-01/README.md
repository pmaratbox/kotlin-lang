# 0287 — 0/1 Knapsack

Maximize value within capacity 5 for items (w,v)=(2,3),(3,4),(4,5), printing `7`. Iterating capacity downward keeps each item used at most once in Kotlin.

## Run

    kotlinc Knapsack01.kt -include-runtime -d knapsack01.jar && java -jar knapsack01.jar
