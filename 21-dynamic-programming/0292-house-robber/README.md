# 0292 — House Robber

Maximize the sum of non-adjacent picks from [2,7,9,3,1], printing `12`. Two rolling vars track the rob/skip states in constant space in Kotlin.

## Run

    kotlinc HouseRobber.kt -include-runtime -d houserobber.jar && java -jar houserobber.jar
