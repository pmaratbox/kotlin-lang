# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. The class implements Kotlin's `Iterator<Int>` protocol with `hasNext()`/`next()`.

## Run

    kotlinc IteratorPattern.kt -include-runtime -d iteratorpattern.jar && java -jar iteratorpattern.jar
