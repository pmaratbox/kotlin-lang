# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A `when` expression maps the name to an idiomatic Kotlin object implementing the interface.

## Run

    kotlinc FactoryMethod.kt -include-runtime -d factorymethod.jar && java -jar factorymethod.jar
