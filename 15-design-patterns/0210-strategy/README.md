# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Kotlin function types `(Int, Int) -> Int` serve as lightweight interchangeable strategies.

## Run

    kotlinc Strategy.kt -include-runtime -d strategy.jar && java -jar strategy.jar
