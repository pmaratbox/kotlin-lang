# 0258 — Count Trailing Zeros

Count the trailing zero bits of 8 (1000), printing `3`. Kotlin's stdlib `Int.countTrailingZeroBits()` returns the answer directly.

## Run

    kotlinc CountTrailingZeros.kt -include-runtime -d counttrailingzeros.jar && java -jar counttrailingzeros.jar
