# 0253 — Power of Two Check

Test whether 16 (yes) and 18 (no) are powers of two, printing `yes no`. A single-expression function returns `n > 0 && (n and (n - 1)) == 0`.

## Run

    kotlinc PowerOfTwoCheck.kt -include-runtime -d poweroftwocheck.jar && java -jar poweroftwocheck.jar
