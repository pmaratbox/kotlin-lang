# 0256 — Hamming Distance

Compute the bit Hamming distance between 1 and 4, printing `2`. Kotlin's stdlib `Int.countOneBits()` counts the set bits of the XOR.

## Run

    kotlinc HammingDistance.kt -include-runtime -d hammingdistance.jar && java -jar hammingdistance.jar
