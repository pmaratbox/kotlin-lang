# 0262 — Parity Check

Compute the bit parity (XOR of all bits) of 7 (->1) and 5 (->0), printing `1 0`. The parity is `countOneBits() and 1` on each Int.

## Run

    kotlinc ParityCheck.kt -include-runtime -d paritycheck.jar && java -jar paritycheck.jar
