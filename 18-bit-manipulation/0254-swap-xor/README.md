# 0254 — XOR Swap

Swap the values 3 and 5 using only XOR (no temporary), printing `5 3`. Kotlin's infix `xor` chains three reassignments on `var` locals.

## Run

    kotlinc SwapXor.kt -include-runtime -d swapxor.jar && java -jar swapxor.jar
