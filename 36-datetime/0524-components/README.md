# 0524 — Date components

Using `kotlinx-datetime`, parse the fixed date `2026-06-15` into a `LocalDate` and extract its components with the library's accessors `year`, `monthNumber`, and `dayOfMonth`, printing each on its own line.

## Run

    kotlin Components.main.kts
