# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array-backed heap sifts up on push and sifts down on pop, with `also` for the in-place swaps.

## Run

    kotlinc HeapManual.kt -include-runtime -d heapmanual.jar && java -jar heapmanual.jar
