# 0437 — Connect & query

Open an in-memory SQLite database with the real `org.xerial:sqlite-jdbc` driver over raw JDBC, run the single query `select 42`, and print the integer result from the `ResultSet`.

## Run

    kotlin ConnectAndQuery.main.kts
